/**
 * The Processing Sketch setup method.
 */
void setup() {
  size(640, 360);
  background(0);
}

/**
 * The Processing Sketch draw method.
 */
void draw() {
  fill(255, 0, 0);
  ellipse(0, 0, height, height);
}
